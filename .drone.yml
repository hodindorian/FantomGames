kind: pipeline
type: docker
name: FantomGames

trigger:
  event:
    - push

steps:

  # build flutter app
  - name: flutter-web-build
    image: ghcr.io/cirruslabs/flutter:3.13.4
    commands:
      - cd ./fantom_games
      - flutter build web --no-tree-shake-icons

    # testing flutter code
  - name: flutter-test
    image: ghcr.io/cirruslabs/flutter:3.13.4
    commands:
      - cd ./fantom_games
      - flutter pub get
      - flutter test
    depends_on: [ flutter-web-build ]

    # deploy sonar
  - name: sonar_flutter
    image: ghcr.io/cirruslabs/flutter:3.13.4
    environment:
      SONAR_TOKEN:
        from_secret: SONAR_TOKEN
    settings:
      sources: ./fantom_games
    commands:
      - export SONAR_SCANNER_VERSION=4.7.0.2747
      - export SONAR_SCANNER_HOME=$HOME/.sonar/sonar-scanner-$SONAR_SCANNER_VERSION-linux
      - curl --create-dirs -sSLo $HOME/.sonar/sonar-scanner.zip https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-$SONAR_SCANNER_VERSION-linux.zip
      - unzip -o $HOME/.sonar/sonar-scanner.zip -d $HOME/.sonar/
      - export PATH=$SONAR_SCANNER_HOME/bin:$PATH
      - export SONAR_SCANNER_OPTS="-server"
      - sonar-scanner -D sonar.projectKey=FantomGames -D sonar.sources=. -D sonar.host.url=https://codefirst.iut.uca.fr/sonar
    depends_on: [ flutter-test ]
    
  # deploy web app docker image
  - name: flutter_deploy_firebase
    image: ghcr.io/cirruslabs/flutter:3.13.4
    settings:
      firebase_login:
        from_secret: FIREBASE_LOGIN
    commands:
      - cd ./fantom_games
      - export GOOGLE_APPLICATION_CREDENTIALS=./lib/config/fantom-games-b26540f25b79.json
      - flutter pub get
      - curl -sL https://firebase.tools | bash
      - firebase experiments:enable webframeworks
      - firebase use fantom_games
      - firebase deploy
    depends_on: [ flutter-web-build ]